# 发布指南

## 📦 发布前检查清单

在发布之前，确保完成以下步骤：

### 1. 登录 npm 账号

```bash
npm login
```

或者如果还没有账号：

```bash
npm adduser
```

**验证登录状态：**

```bash
npm whoami
```

应该显示你的 npm 用户名。

### 2. 检查包维护者权限

确保你登录的账号是包的维护者：

```bash
npm owner ls git-multi-sync-tool
```

如果你的账号不在维护者列表中，需要：

- 使用正确的维护者账号登录（samuelyang066）
- 或者联系维护者添加你为协作者

### 3. 检查版本

运行版本检查：

```bash
npm run check-version
```

这会检查当前版本是否已在 npm 上存在。

### 4. 完整发布前检查

运行认证和版本检查：

```bash
npm run check-auth    # 检查登录状态和权限
npm run check-version # 检查版本冲突
```

### 5. 发布

当所有检查通过后：

```bash
npm publish
```

## 🔧 发布流程

执行 `npm publish` 时会自动运行以下步骤：

1. **认证检查** - 验证是否登录且是否有发布权限
2. **版本检查** - 检查版本是否已存在
3. **清理** - 删除旧的构建文件
4. **编译** - 编译 TypeScript 代码
5. **测试** - 运行单元测试
6. **发布** - 如果以上都通过，发布到 npm

## ❌ 常见错误

### 错误 1: 401 Unauthorized

```
npm error 401 Unauthorized
```

**原因：** 未登录 npm 账号

**解决：**

```bash
npm login
```

### 错误 2: 404 Not Found（PUT 请求）

```
npm error 404 Not Found - PUT https://registry.npmjs.org/git-multi-sync-tool
```

**可能原因：**

1. 未登录 npm 账号
2. 当前用户不是包的维护者
3. 包名不存在且用户没有权限创建新包

**解决：**

1. 确保已登录正确的账号：`npm login`
2. 检查是否是维护者：`npm owner ls git-multi-sync-tool`
3. 如果是首次发布，确保包名可用：`npm search git-multi-sync-tool`

### 错误 3: 版本已存在

```
❌ 错误: 版本 1.0.5 已经在npm上存在！
```

**解决：**

```bash
npm version patch   # 1.0.5 -> 1.0.6
npm version minor   # 1.0.5 -> 1.1.0
npm version major   # 1.0.5 -> 2.0.0
```

### 错误 4: 权限不足

```
❌ 错误: 当前用户 xxx 不是包的维护者
```

**解决：**

- 使用正确的维护者账号登录
- 或者联系维护者添加你为协作者：
  ```bash
  npm owner add <your-username> git-multi-sync-tool
  ```

## 📝 发布命令参考

```bash
# 检查当前登录用户
npm whoami

# 查看包的维护者列表
npm owner ls git-multi-sync-tool

# 添加协作者（需要维护者权限）
npm owner add <username> git-multi-sync-tool

# 更新版本号
npm version patch   # 补丁版本 1.0.5 -> 1.0.6
npm version minor   # 次版本 1.0.5 -> 1.1.0
npm version major   # 主版本 1.0.5 -> 2.0.0

# 发布前完整检查
npm run check-auth
npm run check-version

# 发布（会自动执行所有检查）
npm publish

# 发布到特定标签
npm publish --tag beta
```

## 🚀 快速发布流程

```bash
# 1. 登录
npm login

# 2. 更新版本（如果需要）
npm version patch

# 3. 发布
npm publish
```

## ⚠️ 重要提示

1. **确保使用正确的账号** - 包的维护者账号是 `samuelyang066`
2. **版本号不能重复** - 每次发布必须使用新版本号
3. **测试通过才能发布** - `prepublishOnly` 会运行测试，失败则不会发布
4. **双因素认证** - 如果启用了 2FA，可能需要额外的认证步骤
